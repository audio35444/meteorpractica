import { Meteor } from 'meteor/meteor';
import {Tracker} from 'meteor/tracker';


Meteor.startup(() => {
  // Tracker.autorun(function(){
  //   var cantidad = Posts.find().lenght;
  //   console.log(cantidad);
  // });
});
